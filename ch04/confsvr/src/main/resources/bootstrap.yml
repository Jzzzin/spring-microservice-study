spring:
  application:
    name: configserver
    # eureka 에 등록할 서비스의 논리 이름 (application ID)
  cloud:
    config:
      server:
        encrypt.enabled: false
#encrypt 관련 설정은 bootstrap.yml 에 해야 함
encrypt:
  key: "IMSYMMETRIC"