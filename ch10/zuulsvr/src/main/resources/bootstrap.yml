spring:
  application:
    name: zuulservice
  profiles:
    active: default
  cloud:
    config:
      enabled: true
#      discovery: # config server 2개 이상인 경우
#        enabled: true
#        service-id: configserver
